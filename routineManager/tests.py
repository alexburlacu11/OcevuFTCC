from django.test import TestCase
from _code_dump.routineManager.models import Request, Album, Sequence

# Create your tests here.


# r = Request(id=1, name="x")
# 
# plan = Plan()
# a = Album(id=1, plan=plan)
# s = Sequence(id=1, album = a)